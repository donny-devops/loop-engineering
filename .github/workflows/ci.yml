name: CI
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - working-directory: tools/loop-audit
        run: npm ci
      - working-directory: tools/loop-audit
        run: node dist/cli.js .. --level L2
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - working-directory: tools/loop-audit
        run: npm ci
      - working-directory: tools/loop-audit
        run: npm run build
# cache-bust 20260630-055332
