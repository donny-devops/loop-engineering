# Loop Budget

Daily token cap: 60000
Kill switch: pause job if daily token spend exceeds cap across all loop jobs.
Escalation: notify human if budget hit.

## Tracking
- Append spend summary to `loop-run-log.md` after each run.
- Human reviews `loop-run-log.md` weekly.
